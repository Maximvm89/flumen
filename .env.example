# Copy to .env and fill in. .env is gitignored — never commit real credentials.

SFTP_HOST=sftp.example.com
SFTP_PORT=22
SFTP_USER=pipeline
# Use a password OR a private key (key takes precedence if both are set).
SFTP_PASSWORD=
SFTP_KEY_FILE=
SFTP_KEY_PASSPHRASE=
