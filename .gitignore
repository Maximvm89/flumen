# --- secrets & machine-local config (NEVER commit) ---
.env
config.yaml
pipeline_config/notifications.json

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
build/
dist/

# --- local-only working notes (never commit) ---
/CLAUDE.md
/docs/ARCHITECTURE.md

# --- OS cruft ---
.DS_Store
Thumbs.db

# --- generated / downloaded artifacts (rebuildable) ---
*.ocio
color_pipeline/config.ocio
legami_pipeline.zip

# --- locally synced project copies (data, not code) ---
/LEGAMI/
/Legami/
_tt_frames/
